# Middleware Chain Map
> Auto-generated by `php artisan htms:index` — 2026-03-24 03:49

## Portal Sinh Hoạt (/portal)

```
auth → CheckPortalAccess:activities
  └─ portal.access:slug,activities → PortalAccessMiddleware → PortalService::canAccess()
```

- **Entry Middleware:** `app/Http/Middleware/CheckPortalAccess.php`
- **Feature Middleware:** `app/Http/Middleware/PortalAccessMiddleware.php`

## Portal Mục Vụ (/ministry)

```
auth → EnsureMinistryContext
  └─ portal.access:slug,ministry → PortalAccessMiddleware
```

- **Entry Middleware:** `app/Http/Middleware/EnsureMinistryContext.php`
- **Feature Middleware:** `app/Http/Middleware/PortalAccessMiddleware.php`

## Portal Chấp Sự (/deacon)

```
auth → EnsureDeaconContext
  └─ portal.access:slug,leadership → PortalAccessMiddleware
```

- **Entry Middleware:** `app/Http/Middleware/EnsureDeaconContext.php`
- **Feature Middleware:** `app/Http/Middleware/PortalAccessMiddleware.php`

## Portal Tài Chính (/finance-portal)

```
auth → EnsureFinanceContext
  └─ (context-based, no feature middleware)
```

- **Entry Middleware:** `app/Http/Middleware/EnsureFinanceContext.php`
- **Feature Middleware:** `N/A`

## Portal Tín Hữu (/member)

```
auth only
  └─ (no feature access control)
```

- **Entry Middleware:** `N/A`
- **Feature Middleware:** `N/A`

## Admin Routes (/admin/*)

```
auth → EnsureSuperAdmin
  └─ (superadmin bypasses all)
```

- **Entry Middleware:** `app/Http/Middleware/EnsureSuperAdmin.php`
- **Feature Middleware:** `N/A`

## Global (Every Request)

```
HandleInertiaRequests → injects:
  auth.user, auth.allowed_features, allAvailableDepartments
```

- **File:** `app/Http/Middleware/HandleInertiaRequests.php`
- `auth.allowed_features` = `PortalService::getAllowedFeaturesForDept(user, activeDeptId)`

## MAC V2 Access Service

| Method | Purpose | File |
|--------|---------|------|
| `canAccess(user, deptId, slug)` | Gate per-request | `PortalService.php` |
| `getAllowedFeaturesForDept(user, deptId)` | Sidebar/card list | `PortalService.php` |
| `isFeatureEnabledForDepartment(dept, slug)` | Level 1 check | `FeatureAssignmentService.php` |
| `getAvailableFeaturesForDepartment(dept)` | Level 1 full map | `FeatureAssignmentService.php` |
